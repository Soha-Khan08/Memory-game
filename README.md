# Memory-game
Java card‑matching game asks players to pick two cards, shows them face up, hides non‑matches, and tracks time/moves. Displays “You Win” with records when all pairs match. Errors are handled, win detection tested with JUnit, and code uses clear names, comments, and modular methods.
